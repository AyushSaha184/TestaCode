import { Card } from "@/components/common/Card";

export function SettingsPage() {
  return (
    <Card>
      <h3 className="text-lg font-semibold text-white">Settings</h3>
      <p className="mt-2 text-sm text-slate-400">Configure API environment, notification rules, and team preferences here in a future iteration.</p>
    </Card>
  );
}
